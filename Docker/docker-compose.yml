version: "3.8"
services:
  ansible:
    build:
      context: ./
      dockerfile: Dockerfile
    image: ansible
    container_name: ansible
    hostname: ansible
    tty: true
    restart: always
    volumes:
      - type: bind
        source: ./
        target: /opt/ansible
    working_dir: /opt/ansible
